package busquedaEnCatalogo;

public abstract class CriterioBinario implements Criterio{
	
	protected Criterio c1;
	protected Criterio c2;
	
	// Aqui deberia estar el metodo que implementa de la interfaz
	// Pero como se trata de una clase abstracta no hace falta (aparentemente)
}
